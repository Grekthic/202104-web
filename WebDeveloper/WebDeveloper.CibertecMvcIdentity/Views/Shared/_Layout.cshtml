<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <header>
        @if (User.Identity.IsAuthenticated)
        {
            <form asp-action="CerrarSesion" asp-controller="Cuentas">
                <input type="submit" value="Cerrar Sesion" />
            </form>
        }
    </header>
    <div>
        @RenderBody()
    </div>
</body>
</html>
